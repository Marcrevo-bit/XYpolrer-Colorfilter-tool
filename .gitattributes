# 本仓库的 Git 属性配置：规定 Git 如何处理/比较各类文件，
# 目的是让 Windows(开发者) 与 CI/Linux 环境对文件有一致的解读，避免无意义的 diff 噪声。
#
# 1) 所有文本文件统一按 LF 行尾归一化（checkout 时按平台，仓库内一律 LF），
#    这样在 Windows 上用 CRLF 编辑也不会污染跨平台 diff。
# 2) 把图片等二进制资源显式标记为 binary，
#    防止 Git 误把它们当文本去 diff/合并（否则会产生大段乱码 diff 甚至损坏文件）。
* text=auto eol=lf

# 二进制资源，明确标记防止被当文本处理
*.png binary
*.ico binary
